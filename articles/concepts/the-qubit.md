---
title: A qubit | Microsoft Docs
description: A qubit
author: QuantumWriter
uid: microsoft.quantum.concepts.qubit
ms.author: nawiebe@microsoft.com
ms.date: 12/11/2017
ms.topic: article
ms.openlocfilehash: f29319c3ec19fecc45f5a9f7c16061b9aa9f71ec
ms.sourcegitcommit: 8becfb03eb60ba205c670a634ff4daa8071bcd06
ms.translationtype: MT
ms.contentlocale: hu-HU
ms.lasthandoff: 10/26/2019
ms.locfileid: "73183641"
---
# <a name="the-qubit"></a><span data-ttu-id="e13c0-103">A Qubit</span><span class="sxs-lookup"><span data-stu-id="e13c0-103">The Qubit</span></span>

<span data-ttu-id="e13c0-104">Akárcsak a BITS a klasszikus számítástechnikai információk alapvető információi, a [*qubits*](https://en.wikipedia.org/wiki/Qubit) (Quantum BITS) a kvantum-számítástechnika alapvető információi.</span><span class="sxs-lookup"><span data-stu-id="e13c0-104">Just as bits are the fundamental object of information in classical computing, [*qubits*](https://en.wikipedia.org/wiki/Qubit) (quantum bits) are the fundamental object of information in quantum computing.</span></span>  <span data-ttu-id="e13c0-105">A levelezés megismeréséhez nézzük meg a legegyszerűbb példát: egyetlen qubit.</span><span class="sxs-lookup"><span data-stu-id="e13c0-105">To understand this correspondence, let's look at the simplest example: a single qubit.</span></span>

## <a name="representing-a-qubit"></a><span data-ttu-id="e13c0-106">Qubit jelölő</span><span class="sxs-lookup"><span data-stu-id="e13c0-106">Representing a Qubit</span></span>

<span data-ttu-id="e13c0-107">Egy kis-vagy bináris számjegy esetében akár $0 $ vagy $1 $ értékű is lehet, a qubit értéke a következők egyike lehet, vagy pedig a $0 $ és a $1 $ érték kvantum-kiosztása.</span><span class="sxs-lookup"><span data-stu-id="e13c0-107">While a bit, or binary digit, can have value either $0$ or $1$, a qubit can have a value that is either of these or a quantum superposition of $0$ and $1$.</span></span>

<span data-ttu-id="e13c0-108">Egyetlen qubit állapotát az egység normájának kétdimenziós oszlopos vektora írja le, azaz a bejegyzéseinek nagyságrendjét a $1 $ értékre kell állítani.</span><span class="sxs-lookup"><span data-stu-id="e13c0-108">The state of a single qubit can be described by a two-dimensional column vector of unit norm, that is, the magnitude squared of its entries must sum to $1$.</span></span> <span data-ttu-id="e13c0-109">Ez a "Quantum State Vector" nevű vektor tartalmazza az összes olyan információt, amely az qubit kvantum-rendszer leírásához szükséges, ugyanúgy, mint egyetlen bit a bináris változók állapotának leírásához szükséges összes információt tartalmazza.</span><span class="sxs-lookup"><span data-stu-id="e13c0-109">This vector, called the quantum state vector, holds all the information needed to describe the one-qubit quantum system just as a single bit holds all of the information needed to describe the state of a binary variable.</span></span>

<span data-ttu-id="e13c0-110">A valós vagy komplex számú, a $1 $-os normával rendelkező kétdimenziós oszlop a qubit által tárolt lehetséges kvantum-állapotot jelöli.</span><span class="sxs-lookup"><span data-stu-id="e13c0-110">Any two-dimensional column vector of real or complex numbers with norm $1$ represents a possible quantum state held by a qubit.</span></span> <span data-ttu-id="e13c0-111">Így a $ \begin{bmatrix} \alpha \\\\ \beta \end{bmatrix} $ qubit-állapotot jelöl, ha a $ \alpha $ és a $ \beta $ olyan összetett számok, amelyek megfelelnek a $ | \alpha | ^ 2 + | \beta | ^ 2 = $1.</span><span class="sxs-lookup"><span data-stu-id="e13c0-111">Thus $\begin{bmatrix} \alpha \\\\  \beta \end{bmatrix}$ represents a qubit state if $\alpha$ and $\beta$ are complex numbers satisfying $|\alpha|^2 + |\beta|^2 = 1$.</span></span> <span data-ttu-id="e13c0-112">Néhány példa a qubits jelölő érvényes kvantum-állapotú vektorokra</span><span class="sxs-lookup"><span data-stu-id="e13c0-112">Some examples of valid quantum state vectors representing qubits include</span></span>

<span data-ttu-id="e13c0-113">$ $ \begin{bmatrix} 1 \\\\ 0 \end{bmatrix}, \begin{bmatrix} 0 \\\\ 1 \end{bmatrix}, \begin{bmatrix} \frac{1}{\sqrt{2}} \\\\ \frac{1}{\sqrt{2}} \end{bmatrix} , \begin{bmatrix} \frac{1}{\sqrt{2}} \\\\ \frac{-1}{\sqrt{2}} \end{bmatrix}, \text{és} \begin{bmatrix} \frac{1}{\sqrt{2}} \\\\ \frac{i}{\sqrt{2}} \end{ bmatrix}. $ $</span><span class="sxs-lookup"><span data-stu-id="e13c0-113">$$\begin{bmatrix} 1 \\\\  0 \end{bmatrix}, \begin{bmatrix} 0 \\\\  1 \end{bmatrix}, \begin{bmatrix} \frac{1}{\sqrt{2}} \\\\  \frac{1}{\sqrt{2}} \end{bmatrix}, \begin{bmatrix} \frac{1}{\sqrt{2}} \\\\  \frac{-1}{\sqrt{2}} \end{bmatrix}, \text{ and }\begin{bmatrix} \frac{1}{\sqrt{2}} \\\\  \frac{i}{\sqrt{2}} \end{bmatrix}.$$</span></span>

<span data-ttu-id="e13c0-114">A Quantum State vectors $ \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} $ és $ \begin{bmatrix} 0 \\\\ 1 \end{bmatrix} $, és egy speciális szerepkört is igénybe vehetik.</span><span class="sxs-lookup"><span data-stu-id="e13c0-114">The quantum state vectors $\begin{bmatrix} 1 \\\\  0 \end{bmatrix}$ and $\begin{bmatrix} 0 \\\\  1 \end{bmatrix}$ take a special role.</span></span> <span data-ttu-id="e13c0-115">Ez a két vektor képezi a qubit állapotát leíró vektoros terület alapját.</span><span class="sxs-lookup"><span data-stu-id="e13c0-115">These two vectors form a basis for the vector space that describes the qubit's state.</span></span> <span data-ttu-id="e13c0-116">Ez azt jelenti, hogy a kvantum-állapot vektora ezen alapul szolgáló vektorok összegeként írható.</span><span class="sxs-lookup"><span data-stu-id="e13c0-116">This means that any quantum state vector can be written as a sum of these basis vectors.</span></span> <span data-ttu-id="e13c0-117">Pontosabban, az \begin{bmatrix} x \\\\ y \end{bmatrix} $ $x \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} + y \begin{bmatrix} 0 \\\\ 1 \end{bmatrix} $ értéket lehet írni.</span><span class="sxs-lookup"><span data-stu-id="e13c0-117">Specifically, the vector $\begin{bmatrix} x \\\\  y \end{bmatrix}$ can be written as $x \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} + y \begin{bmatrix} 0 \\\\  1 \end{bmatrix}$.</span></span> <span data-ttu-id="e13c0-118">Habár ezek a vektorok bármilyen forgása tökéletesen érvényes alapként szolgál a qubit számára, úgy döntünk, hogy ezt a jogosultságot a *számítási alap*meghívásával emeljük ki.</span><span class="sxs-lookup"><span data-stu-id="e13c0-118">While any rotation of these vectors would serve as a perfectly valid basis for the qubit, we choose to privilege this one, by calling it the *computational basis*.</span></span>

<span data-ttu-id="e13c0-119">Ezt a két kvantum-állapotot a klasszikus bit két állapotának, azaz a $0 $ és a $1 $ értéknek kell megfelelnie.</span><span class="sxs-lookup"><span data-stu-id="e13c0-119">We take these two quantum states to correspond to the two states of a classical bit, namely $0$ and $1$.</span></span> <span data-ttu-id="e13c0-120">A standard konvenció a következő választás</span><span class="sxs-lookup"><span data-stu-id="e13c0-120">The standard convention is to choose</span></span>

<span data-ttu-id="e13c0-121">$ $0 \ EQUIV \begin{bmatrix} 1 \\\\ 0 \end{bmatrix}, \qquad 1 \equiv \begin{bmatrix} 0 \\\\ 1 \end{bmatrix}, $ $</span><span class="sxs-lookup"><span data-stu-id="e13c0-121">$$0\equiv \begin{bmatrix} 1 \\\\  0 \end{bmatrix}, \qquad 1 \equiv \begin{bmatrix} 0 \\\\  1 \end{bmatrix},$$</span></span>

<span data-ttu-id="e13c0-122">Habár a szemközti választási lehetőség is megfelelő lehet.</span><span class="sxs-lookup"><span data-stu-id="e13c0-122">although the opposite choice could equally well be taken.</span></span> <span data-ttu-id="e13c0-123">Így a lehetséges qubit kvantum-állapotú vektorok végtelen száma közül csak kettő felel meg a klasszikus bitek állapotának. az összes többi kvantum-állapot nem.</span><span class="sxs-lookup"><span data-stu-id="e13c0-123">Thus, out of the infinite number of possible single-qubit quantum state vectors, only two correspond to states of classical bits; all other quantum states do not.</span></span>

## <a name="measuring-a-qubit"></a><span data-ttu-id="e13c0-124">Qubit mérése</span><span class="sxs-lookup"><span data-stu-id="e13c0-124">Measuring a Qubit</span></span>

<span data-ttu-id="e13c0-125">Most, hogy már tudjuk, hogyan jelentheti a qubit, megtudhatjuk, hogy mit jelentenek ezek az államok a [*mérés*](https://en.wikipedia.org/wiki/Measurement_in_quantum_mechanics)koncepciójának megvitatása során.</span><span class="sxs-lookup"><span data-stu-id="e13c0-125">Now that we know how to represent a qubit, we can gain some intuition for what these states represent by discussing the concept of [*measurement*](https://en.wikipedia.org/wiki/Measurement_in_quantum_mechanics).</span></span> <span data-ttu-id="e13c0-126">Egy mérték megfelel a "looking" qubit, amely azonnal Összecsukja a kvantum-állapotot a két klasszikus állapot közül a $ \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} $ vagy a $ \begin{bmatrix} 0 \\\\ 1 \end{ bmatrix} $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-126">A measurement corresponds to the informal idea of “looking” at a qubit, which immediately collapses the quantum state to one of the two classical states  $\begin{bmatrix} 1 \\\\  0 \end{bmatrix}$ or  $\begin{bmatrix} 0 \\\\  1 \end{bmatrix}$.</span></span> <span data-ttu-id="e13c0-127">Ha a Quantum State Vector $ \begin{bmatrix} \alpha \\\\ \beta \end{bmatrix} $ értékkel megadott qubit mérik, a $0 $ értéket kapjuk meg, a valószínűség: $ | \alpha | ^ 2 $, valamint a $1 $ értéket a valószínűség $ | \beta | ^ 2 $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-127">When a qubit given by the quantum state vector  $\begin{bmatrix} \alpha \\\\  \beta \end{bmatrix}$ is measured, we obtain the outcome $0$ with probability $|\alpha|^2$ and the outcome $1$  with probability $|\beta|^2$.</span></span> <span data-ttu-id="e13c0-128">A $0 $ értéknél a qubit új állapota $ \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} $; $1 $ után az állapota $ \begin{bmatrix} 0 \\\\ 1 \end{bmatrix} $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-128">On outcome $0$, the qubit's new state is $\begin{bmatrix} 1 \\\\  0 \end{bmatrix}$; on outcome $1$ its state is $\begin{bmatrix} 0 \\\\  1 \end{bmatrix}$.</span></span> <span data-ttu-id="e13c0-129">Vegye figyelembe, hogy ezek a valószínűségek összege $1 $, a normalizálás feltétele miatt $ | \alpha | ^ 2 + | \beta | ^ 2 = $1.</span><span class="sxs-lookup"><span data-stu-id="e13c0-129">Note that these probabilities sum up to $1$ because of the normalization condition $|\alpha|^2 + |\beta|^2 = 1$.</span></span>

<span data-ttu-id="e13c0-130">A mérés tulajdonságai azt is jelenti, hogy a kvantum-állapot vektorának általános aláírása lényegtelen.</span><span class="sxs-lookup"><span data-stu-id="e13c0-130">The properties of measurement also mean that the overall sign of the quantum state vector is irrelevant.</span></span> <span data-ttu-id="e13c0-131">A vektor megtagadásával egyenértékű a $ \alpha \rightarrow-\alpha $ és a $ \beta \rightarrow-\beta $ értékkel.</span><span class="sxs-lookup"><span data-stu-id="e13c0-131">Negating a vector is equivalent to $\alpha \rightarrow -\alpha$ and $\beta \rightarrow -\beta$.</span></span> <span data-ttu-id="e13c0-132">Mivel a $0 $ és a $1 $ mérés valószínűsége a feltételek méretétől függ, az ilyen jelek beillesztése nem változtatja meg a valószínűségeket.</span><span class="sxs-lookup"><span data-stu-id="e13c0-132">Because the probability of measuring $0$ and $1$ depends on the magnitude squared of the terms, inserting such signs does not change the probabilities whatsoever.</span></span> <span data-ttu-id="e13c0-133">Az ilyen fázisokat általában [\`\`*globális fázisokban*(""](https://en.wikipedia.org/wiki/Phase_factor) ), és általánosabban lehet $e ^ {i \phi} $, nem csupán $ \pm $1.</span><span class="sxs-lookup"><span data-stu-id="e13c0-133">Such phases are commonly called [\`\`*global phases*''](https://en.wikipedia.org/wiki/Phase_factor) and more generally can be of the form $e^{i \phi}$ rather than just $\pm 1$.</span></span>

<span data-ttu-id="e13c0-134">A mérés utolsó fontos tulajdonsága, hogy nem szükségszerűen károsítja az összes kvantum-állapotú vektort.</span><span class="sxs-lookup"><span data-stu-id="e13c0-134">A final important property of measurement is that it does not necessarily damage all quantum state vectors.</span></span> <span data-ttu-id="e13c0-135">Ha a \begin{bmatrix} \\1. állapotú qubit kezdődnek, \\ 0 \end{bmatrix} $, amely a klasszikus állapot $0 $-nak felel meg, akkor a mérés ez az állapot mindig a $0 $ eredményt adja, és változatlanul hagyja a kvantum-állapotot.</span><span class="sxs-lookup"><span data-stu-id="e13c0-135">If we start with a qubit in the state $\begin{bmatrix} 1 \\\\  0 \end{bmatrix}$, which corresponds to the classical state $0$, measuring this state will always yield the outcome $0$ and leave the quantum state unchanged.</span></span> <span data-ttu-id="e13c0-136">Ebben az értelemben, ha csak a klasszikus BITS (azaz a qubits $ \begin{bmatrix}1 \\\\ 0 \end{bmatrix} $ vagy $ \begin{bmatrix}0 \\\\ 1 \end{bmatrix} $), akkor a mérés nem károsítja a rendszerét.</span><span class="sxs-lookup"><span data-stu-id="e13c0-136">In this sense, if we only have classical bits (i.e., qubits that are either $\begin{bmatrix}1 \\\\  0 \end{bmatrix}$ or $\begin{bmatrix}0 \\\\  1 \end{bmatrix}$) then measurement does not damage the system.</span></span> <span data-ttu-id="e13c0-137">Ez azt jelenti, hogy a klasszikus adatmennyiséget replikálhatja, és a kvantum-számítógépeken ugyanúgy kezelheti azt, mint egy klasszikus számítógépen.</span><span class="sxs-lookup"><span data-stu-id="e13c0-137">This means that we can replicate classical data and manipulate it on a quantum computer just as one could do on a classical computer.</span></span> <span data-ttu-id="e13c0-138">Azonban az adatok egyszerre való tárolása is lehetséges, hogy a kvantum-számítástechnika a lehető leghamarabb megemeli a kvantum-adatok válogatás nélkül történő másolásának lehetőségét, továbbá a [nem klónozási tételről is tájékozódhat. ](https://en.wikipedia.org/wiki/No-cloning_theorem).</span><span class="sxs-lookup"><span data-stu-id="e13c0-138">The ability, however, to store information in both states at once is what elevates quantum computing beyond what is possible classically and further robs quantum computers of the ability to copy quantum data indiscriminately, see also [the no-cloning theorem](https://en.wikipedia.org/wiki/No-cloning_theorem).</span></span>

## <a name="visualizing-qubits-and-transformations-using-the-bloch-sphere"></a><span data-ttu-id="e13c0-139">Qubits és átalakítások megjelenítése a Bloch szférával</span><span class="sxs-lookup"><span data-stu-id="e13c0-139">Visualizing Qubits and Transformations using the Bloch Sphere</span></span>

<span data-ttu-id="e13c0-140">A qubits a [*Bloch szféra*](https://en.wikipedia.org/wiki/Bloch_sphere) képviseletének használatával is megjelenhet a 3 $ D-ben.</span><span class="sxs-lookup"><span data-stu-id="e13c0-140">Qubits may also be pictured in $3$D using the [*Bloch sphere*](https://en.wikipedia.org/wiki/Bloch_sphere) representation.</span></span>  <span data-ttu-id="e13c0-141">A Bloch szférában egy qubit kvantum-állapot (amely egy kétdimenziós, összetett vektor), amely háromdimenziós, valós értékű vektor.</span><span class="sxs-lookup"><span data-stu-id="e13c0-141">The Bloch sphere gives a way of describing a single-qubit quantum state (which is a two-dimensional complex vector) as a three-dimensional real-valued vector.</span></span>  <span data-ttu-id="e13c0-142">Ez azért fontos, mert lehetővé teszi, hogy megjelenítse az qubit állapotokat, és ezáltal a qubit állapotok megismerése szempontjából felbecsülhetetlen értékűek legyenek.</span><span class="sxs-lookup"><span data-stu-id="e13c0-142">This is important because it allows us to visualize single-qubit states and thereby develop reasoning that can be invaluable in understanding multi-qubit states (where sadly the Bloch sphere representation breaks down).</span></span>  <span data-ttu-id="e13c0-143">A Bloch gömb a következőképpen látható:</span><span class="sxs-lookup"><span data-stu-id="e13c0-143">The Bloch sphere can be visualized as follows:</span></span>

<!--- ![](.\media\bloch.svg){ width=50% } --->
<span data-ttu-id="e13c0-144">![Bloch gömb](~/media/concepts_bloch.png)</span><span class="sxs-lookup"><span data-stu-id="e13c0-144">![Bloch sphere](~/media/concepts_bloch.png)</span></span>

<span data-ttu-id="e13c0-145">Az ábrán látható nyilak azt mutatják be, hogy a kvantum-állapot vektora hogyan mutat, és a nyíl minden átalakítása a kardinális tengelyek egyikére mutat.</span><span class="sxs-lookup"><span data-stu-id="e13c0-145">The arrows in this diagram show the direction in which the quantum state vector is pointing and each transformation of the arrow can be thought of as a rotation about one of the cardinal axes.</span></span>
<span data-ttu-id="e13c0-146">Miközben a kvantum-számítási folyamatra gondol, mivel a Forgások sorozatából egy hatékony intuíció van, nagy kihívást jelent az algoritmusok megtervezése és leírása.</span><span class="sxs-lookup"><span data-stu-id="e13c0-146">While thinking about a quantum computation as a sequence of rotations is a powerful intuition, it is challenging to use this intuition to design and describe algorithms.</span></span> <span data-ttu-id="e13c0-147">A Q # ezt a problémát úgy enyhíti, ha az ilyen elforgatások leírására szolgáló nyelvet biztosít.</span><span class="sxs-lookup"><span data-stu-id="e13c0-147">Q# alleviates this issue by providing a language for describing such rotations.</span></span>

## <a name="single-qubit-operations"></a><span data-ttu-id="e13c0-148">Qubit műveletek</span><span class="sxs-lookup"><span data-stu-id="e13c0-148">Single-Qubit Operations</span></span>

<span data-ttu-id="e13c0-149">A kvantum-számítógépek a kvantum-kapuk univerzális készletének használatával dolgozzák fel az adatfeldolgozást, amely a kvantum-állapot vektorának bármilyen forgását Emulálhatja.</span><span class="sxs-lookup"><span data-stu-id="e13c0-149">Quantum computers process data by applying a universal set of quantum gates that can emulate any rotation of the quantum state vector.</span></span>
<span data-ttu-id="e13c0-150">Ez az egyetemességi koncepció hasonlít a hagyományos (azaz klasszikus) számítástechnikai koncepcióhoz, ahol a kapuk univerzálisnak tekintendők, ha a bemeneti bitek minden átalakítása véges hosszúságú áramkör használatával végezhető el.</span><span class="sxs-lookup"><span data-stu-id="e13c0-150">This notion of universality is akin to the notion of universality for traditional (i.e., classical) computing where a gate set is considered to be universal if every transformation of the input bits can be performed using a finite length circuit.</span></span>
<span data-ttu-id="e13c0-151">A kvantum-számítástechnika esetében a qubit elvégezhető érvényes átalakítások egységes átalakítások és mérések.</span><span class="sxs-lookup"><span data-stu-id="e13c0-151">In quantum computing, the valid transformations that we are allowed to perform on a qubit are unitary transformations and measurement.</span></span>
<span data-ttu-id="e13c0-152">A *adjoint művelet* vagy az összetett konjugátum átültetése rendkívül fontos a Quantum Computing számára, mivel ez szükséges a kvantum-átalakítások megfordításához.</span><span class="sxs-lookup"><span data-stu-id="e13c0-152">The *adjoint operation* or the complex conjugate transpose is of crucial importance to quantum computing because it is needed to invert quantum transformations.</span></span>
<span data-ttu-id="e13c0-153">A Q # ezt a módszert mutatja be azáltal, hogy olyan metódusokat biztosít, amelyek automatikusan lefordítják a Gate-sorozatot a adjoint, ami sok esetben menti a programozót, hogy a adjoints.</span><span class="sxs-lookup"><span data-stu-id="e13c0-153">Q# reflects this by providing methods to automatically compile gate sequences to their adjoint, which saves the programmer from having to hand code adjoints in many cases.</span></span> <span data-ttu-id="e13c0-154">Alább látható egy példa:</span><span class="sxs-lookup"><span data-stu-id="e13c0-154">An example of this is shown below:</span></span>

```qsharp
operation PrepareSuperposition(qubit : Qubit) : Unit
is Adj { // Auto-generate the adjoint of the operation
    H(qubit);
}
```

<span data-ttu-id="e13c0-155">Bár ez egy triviális példa (mivel a <xref:microsoft.quantum.intrinsic.h> művelet önadjoint), láthatja, hogy ez felbecsülhetetlen értékűebb qubit-műveletekből válik.</span><span class="sxs-lookup"><span data-stu-id="e13c0-155">Although this is a trivial example (as the <xref:microsoft.quantum.intrinsic.h> operation is self-adjoint), you can see how this becomes invaluable for more complicated qubit operations.</span></span>
<span data-ttu-id="e13c0-156">További információ: [Operations and functions](xref:microsoft.quantum.techniques.opsandfunctions).</span><span class="sxs-lookup"><span data-stu-id="e13c0-156">For more information, see [Operations and Functions](xref:microsoft.quantum.techniques.opsandfunctions).</span></span>

<span data-ttu-id="e13c0-157">A klasszikus számítógépeken csak négy függvény képezhető le egy kicsit.</span><span class="sxs-lookup"><span data-stu-id="e13c0-157">There are only four functions that map one bit to one bit on a classical computer.</span></span> <span data-ttu-id="e13c0-158">Ezzel szemben a kvantum-számítógépek egyetlen qubit végtelen számú egységes átalakítás létezik.</span><span class="sxs-lookup"><span data-stu-id="e13c0-158">In contrast, there are an infinite number of unitary transformations on a single qubit on a quantum computer.</span></span> <span data-ttu-id="e13c0-159">Ezért a " [*Gates*](https://en.wikipedia.org/wiki/Quantum_logic_gate)" nevű egyszerű kvantum-műveletek egyetlen véges halmaza sem tudja pontosan replikálni a kvantum-számítástechnikai szolgáltatásban engedélyezett, egységes átalakítások végtelen készletét.</span><span class="sxs-lookup"><span data-stu-id="e13c0-159">Therefore, no finite set of primitive quantum operations, called [*gates*](https://en.wikipedia.org/wiki/Quantum_logic_gate), can exactly replicate the infinite set of unitary transformations allowed in quantum computing.</span></span> <span data-ttu-id="e13c0-160">Ez azt jelenti, hogy a klasszikus számítástechnikai rendszertől eltérően a kvantum-számítógép nem tudja megvalósítani az összes lehetséges kvantum-programot, amely pontosan véges számú kaput használ.</span><span class="sxs-lookup"><span data-stu-id="e13c0-160">This means, unlike classical computing, it is impossible for a quantum computer to implement every possible quantum program exactly using a finite number of gates.</span></span> <span data-ttu-id="e13c0-161">Így a kvantum-számítógépek nem lehetnek univerzálisak a klasszikus számítógépek azonos értelemben.</span><span class="sxs-lookup"><span data-stu-id="e13c0-161">Thus quantum computers cannot be universal in the same sense of classical computers.</span></span> <span data-ttu-id="e13c0-162">Ennek eredményeképpen, amikor azt mondjuk, hogy a kapuk halmaza *univerzális* a Quantum Computing esetében, valójában némileg gyengébb, mint a klasszikus számítástechnika.</span><span class="sxs-lookup"><span data-stu-id="e13c0-162">As a result, when we say that a set of gates is *universal* for quantum computing we actually mean something slightly weaker than we mean with classical computing.</span></span>
<span data-ttu-id="e13c0-163">Az egyetemesség esetében követelmény, hogy a kvantum-számítógép csak a véges hosszúságú Gate-sorozatot használva *közelítse* meg az összes egységes mátrixot egy véges hibán belül.</span><span class="sxs-lookup"><span data-stu-id="e13c0-163">For universality, we require that a quantum computer only *approximate* every unitary matrix within a finite error using a finite length gate sequence.</span></span>
<span data-ttu-id="e13c0-164">Más szóval, a Gates egy univerzális Gate-készlet, ha bármely egységes átalakítás a készletből származó Gates-termékként is írható.</span><span class="sxs-lookup"><span data-stu-id="e13c0-164">In other words, a set of gates is a universal gate set if any unitary transformation can be approximately written as a product of gates from this set.</span></span> <span data-ttu-id="e13c0-165">Megköveteljük, hogy az előírt hibákhoz kötve legyenek a Gates $G _{1}, a G_{2}, a \ldots, a G_N $ a Gate készletből, hogy</span><span class="sxs-lookup"><span data-stu-id="e13c0-165">We require that for any prescribed error bound, there exist gates $G_{1}, G_{2},\ldots, G_N$ from the gate set such that</span></span>

<span data-ttu-id="e13c0-166">$ $ G_N G_ {N-1} \cdots G_2 G_1 \approx U. $ $</span><span class="sxs-lookup"><span data-stu-id="e13c0-166">$$ G_N G_{N-1} \cdots G_2 G_1 \approx U. $$</span></span>

<span data-ttu-id="e13c0-167">Vegye figyelembe, hogy mivel a mátrixok szorzási konvenciója az, hogy az első kapu műveletét az ebben a sorozatban, $G _N $ értéket adja meg, valójában az utolsó lett alkalmazva a kvantum-állapot vektorára.</span><span class="sxs-lookup"><span data-stu-id="e13c0-167">Note that because the convention for matrix multiplication is to multiply from right to left the first gate operation in this sequence, $G_N$, is actually the last one applied to the quantum state vector.</span></span> <span data-ttu-id="e13c0-168">További formálisan azt mondjuk, hogy egy ilyen Gate-készlet univerzális, ha minden hibatűrés $ \epsilon > 0 $ létezik $G _1, \ldots, G_N $, hogy a $G _N \ ldots G_1 $ és $U $ közötti távolság legfeljebb $ \epsilon $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-168">More formally, we say that such a gate set is universal if for every error tolerance $\epsilon>0$ there exists $G_1,\ldots, G_N$ such that  the distance between $G_N\ldots G_1$ and $U$ is at most $\epsilon$.</span></span> <span data-ttu-id="e13c0-169">Ideális esetben $N $ $ \epsilon $ eléréséhez szükséges értéknek kell megfelelnie a Poly-logaritmikusan és a $1/\ epszilon $ közötti méretnek.</span><span class="sxs-lookup"><span data-stu-id="e13c0-169">Ideally the value of $N$ needed to reach this distance of $\epsilon$ should scale poly-logarithmically with $1/\epsilon$.</span></span>

<span data-ttu-id="e13c0-170">Mit jelent ez az univerzális Gate-készlet a gyakorlatban?</span><span class="sxs-lookup"><span data-stu-id="e13c0-170">What does such a universal gate set look like in practice?</span></span>  <span data-ttu-id="e13c0-171">Az egyszeres qubit kapuk legegyszerűbb univerzális kapuja csak két kapuból áll: a Hadamard Gate $H $ és az úgynevezett $T $-Gate (más néven $ \ PI/8 $ Gate):</span><span class="sxs-lookup"><span data-stu-id="e13c0-171">The simplest such universal gate set for single-qubit gates consists of only two gates: the Hadamard gate $H$ and the so-called $T$-gate (also known as the $\pi/8$ gate):</span></span>

<span data-ttu-id="e13c0-172">$ $ H = \frac{1}{\sqrt{2}} \begin{bmatrix} 1 & 1 \\\\ 1 &-1 \end{bmatrix}, \qquad T = \begin{bmatrix} 1 & 0 \\\\ 0 & e ^ {i \ PI/4} \end{bmatrix}.</span><span class="sxs-lookup"><span data-stu-id="e13c0-172">$$ H=\frac{1}{\sqrt{2}}\begin{bmatrix} 1 & 1 \\\\  1 &-1  \end{bmatrix},\qquad T=\begin{bmatrix} 1 & 0 \\\\  0 & e^{i\pi/4} \end{bmatrix}.</span></span>
$$

<span data-ttu-id="e13c0-173">A kvantum-hibák kijavításával kapcsolatos gyakorlati okok miatt azonban kényelmesebb lehet a nagyobb kapuk készletének megfontolása, azaz az $H $ és a $T $ használatával hozható létre.</span><span class="sxs-lookup"><span data-stu-id="e13c0-173">However, for practical reasons related to quantum error correction it can be more convenient to consider a larger gate set, namely one that can be generated using $H$ and $T$.</span></span>
<span data-ttu-id="e13c0-174">A Quantum Gates két kategóriába sorolhatók: Clifford Gates és $T $-Gate.</span><span class="sxs-lookup"><span data-stu-id="e13c0-174">We can classify the quantum gates into two categories: Clifford gates and the $T$-gate.</span></span>
<span data-ttu-id="e13c0-175">Ez az albontás azért hasznos, mert számos kvantum-hibajavítási sémában az úgynevezett Clifford-kapuk könnyen megvalósítható, ezért nagyon kevés erőforrásra van szükségük a műveletek és a qubits terén a hibatűrő működés megvalósításához, míg a nem Clifford-kapuk rendkívül költséges, ha hibatűrést igényel.</span><span class="sxs-lookup"><span data-stu-id="e13c0-175">This subdivision is useful because in many quantum error correction schemes the so-called Clifford gates are easy to implement, that is they require very few resources in terms of operations and qubits to implement fault tolerantly, whereas non-Clifford gates are quite costly when requiring fault tolerance.</span></span> <span data-ttu-id="e13c0-176">A [Q #-ban alapértelmezés](xref:microsoft.quantum.libraries.standard.prelude)szerint a qubit Clifford Gates standard készlete tartalmazza a következőket:</span><span class="sxs-lookup"><span data-stu-id="e13c0-176">The standard set of single-qubit Clifford gates, [included by default in Q#](xref:microsoft.quantum.libraries.standard.prelude), include</span></span>

<span data-ttu-id="e13c0-177">$ $ H = \frac{1}{\sqrt{2}} \begin{bmatrix} 1 & 1 \\\\ 1 &-1 \end{bmatrix}, \qquad S = \begin{bmatrix} 1 & 0 \\\\ 0 & i \end{bmatrix} = T ^ 2, \qquad X = \begin{bmatrix} 0 & 1 \\\\ 1 & 0 \end{bmatrix} = HT ^ 4H, $ $</span><span class="sxs-lookup"><span data-stu-id="e13c0-177">$$ H=\frac{1}{\sqrt{2}}\begin{bmatrix} 1 & 1 \\\\  1 &-1  \end{bmatrix} ,\qquad S =\begin{bmatrix} 1 & 0 \\\\  0 & i \end{bmatrix}= T^2,\qquad X=\begin{bmatrix} 0 &1 \\\\  1& 0 \end{bmatrix}= HT^4H, $$</span></span>

<span data-ttu-id="e13c0-178">$ $ Y = \begin{bmatrix} 0 &-i \\\\ & 0 \end{bmatrix} = T ^ 2HT ^ 4 HT ^ 6, \qquad Z = \begin{bmatrix}1 & 0\\\\ 0 &-1 \end{bmatrix} = T ^ 4.</span><span class="sxs-lookup"><span data-stu-id="e13c0-178">$$ Y = \begin{bmatrix} 0 & -i \\\\  i & 0 \end{bmatrix}=T^2HT^4  HT^6, \qquad Z=\begin{bmatrix}1&0\\\\ 0&-1 \end{bmatrix}=T^4.</span></span>
$$

<span data-ttu-id="e13c0-179">Itt az Operations $X $, $Y $ és a $Z $ is különösen gyakran használatos, és a megnevezett [*Pauli-operátorok*](https://en.wikipedia.org/wiki/Pauli_matrices) a Wolfgang Pauli létrehozója után.</span><span class="sxs-lookup"><span data-stu-id="e13c0-179">Here the operations $X$, $Y$ and $Z$ are used especially frequently and are named [*Pauli operators*](https://en.wikipedia.org/wiki/Pauli_matrices) after their creator Wolfgang Pauli.</span></span>
<span data-ttu-id="e13c0-180">A nem Clifford-kapuval (a $T $-Gate) együtt ezek a műveletek összeállíthatók egyetlen qubit egységes átalakításának megközelítve.</span><span class="sxs-lookup"><span data-stu-id="e13c0-180">Together with the non-Clifford gate (the $T$-gate), these operations can be composed to approximate any unitary transformation on a single qubit.</span></span>

<span data-ttu-id="e13c0-181">További információ ezekről a műveletekről, a Bloch-szférában végzett képviseletekről és a Q # implementációról: [belső műveletek és függvények](xref:microsoft.quantum.libraries.standard.prelude#intrinsic-operations-and-functions).</span><span class="sxs-lookup"><span data-stu-id="e13c0-181">For more information on these operations, their Bloch sphere representations and Q# implementations, see [Intrinsic Operations and Functions](xref:microsoft.quantum.libraries.standard.prelude#intrinsic-operations-and-functions).</span></span>

<span data-ttu-id="e13c0-182">Példa arra, hogy az egységes átalakítások hogyan használhatók ezekből a primitívekről, a fenti Bloch-szférákban látható három átalakítás a \begin{bmatrix} 1. \\\\ 0 \end{bmatrix} \mapsto HZH \begin{bmatrix} 1 \\\\ 0 \end{bmatrix} = \begin{bmatrix} 0 \\\\ 1 \end{bmatrix} $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-182">As an example of how unitary transformations can be built from these primitives, the three transformations pictured in the Bloch spheres above correspond to the gate sequence $\begin{bmatrix} 1 \\\\  0 \end{bmatrix} \mapsto HZH \begin{bmatrix} 1 \\\\  0 \end{bmatrix} = \begin{bmatrix} 0 \\\\  1 \end{bmatrix}$.</span></span>

<span data-ttu-id="e13c0-183">Míg az előző a legnépszerűbb primitív kapuk, amelyek a verem logikai szintjén lévő műveletek leírására szolgálnak (gondoljunk a logikai szintre, mint a kvantum-algoritmus szintje), gyakran kényelmes, hogy kevesebb alapszintű műveletet lehessen figyelembe venni az algoritmuson. szint, például a függvények leírási szintjéhez közelebbi műveletek.</span><span class="sxs-lookup"><span data-stu-id="e13c0-183">While the previous constitute the most popular primitive gates for describing operations on the logical level of the stack (think of the logical level as the level of the quantum algorithm), it is often convenient to consider less basic operations at the algorithmic level, for example operations closer to a function description level.</span></span> <span data-ttu-id="e13c0-184">Szerencsére a Q # is rendelkezik a magasabb szintű unitaries megvalósításához szükséges módszerekkel, amelyek lehetővé teszik a magas szintű algoritmusok megvalósítását anélkül, hogy a Clifford és a $T $-Gates-t explicit módon el kellene bontani.</span><span class="sxs-lookup"><span data-stu-id="e13c0-184">Fortunately, Q# also has methods available for implementing higher-level unitaries, which in turn allow high-level algorithms to be implemented without explicitly decomposing everything down to Clifford and $T$-gates.</span></span>

<span data-ttu-id="e13c0-185">A legegyszerűbb ilyen primitív az egyetlen qubit.</span><span class="sxs-lookup"><span data-stu-id="e13c0-185">The simplest such primitive is the single qubit-rotation.</span></span> <span data-ttu-id="e13c0-186">A rendszer általában három qubit-rotációt vesz figyelembe: $R _x $, $R _y $ és $R _z $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-186">Three single-qubit rotations are typically considered: $R_x$, $R_y$ and $R_z$.</span></span> <span data-ttu-id="e13c0-187">A rotációs $R _x (\theta) $ művelet működésének megjelenítéséhez tegyük fel, hogy a jobb oldali hüvelykujj a Bloch gömb $x $ tengelyének irányába mutat, és a vektort a kezével $ \ THÉTA/2 $ radián fölé mozgatja.</span><span class="sxs-lookup"><span data-stu-id="e13c0-187">To visualize the action of the rotation $R_x(\theta)$, for example, imagine pointing your right thumb along the direction of the $x$-axis of the Bloch sphere and rotating the vector with your hand through an angle of $\theta/2$ radians.</span></span> <span data-ttu-id="e13c0-188">Ez a $2 $-es zavaros tényező abból ered, hogy a (z) a Bloch-szférában a merőleges vektorok száma 180 ^ \circ $, de a a Bloch-gömbön kívül ténylegesen $90 ^ \circ $ fok.</span><span class="sxs-lookup"><span data-stu-id="e13c0-188">This confusing factor of $2$ arises from the fact that orthogonal vectors are $180^\circ$ apart when plotted on the Bloch sphere, yet are actually $90^\circ$ degrees apart geometrically.</span></span> <span data-ttu-id="e13c0-189">A megfelelő egységes mátrixok a következők:</span><span class="sxs-lookup"><span data-stu-id="e13c0-189">The corresponding unitary matrices are:</span></span>

<span data-ttu-id="e13c0-190">\begin{align *} & R_z (\theta) = e ^ {-I\theta z/2} = \begin{bmatrix} e ^ {-i \ THÉTA/2} & 0\\\\ 0 & e ^ {i \ THÉTA/2} \end{bmatrix}, \\\\ & R_x (\theta) = e ^ {-I\theta x/2} = HR_z (\theta) H = \begin{bmatrix} \cos (\ THÉTA/2) &-i\sin (\ THÉTA/2)\\\\-i\sin (\ THÉTA/2) & \cos (\ THÉTA/2) \end{bmatrix}, \\\\ & R_y (\theta) = e ^ {-i\theta Y/2} = SHR_z (\theta) HS ^ \dagger = \begin{bmatrix} \cos (\ THÉTA/2) &-\sin (\ THÉTA/2) @no__ t_9_ \\ \sin (\ THÉTA/2) & \cos (\ THÉTA/2) \end{bmatrix}. \end{align*}</span><span class="sxs-lookup"><span data-stu-id="e13c0-190">\begin{align *} &R_z(\theta) = e^{-i\theta Z/2} = \begin{bmatrix} e^{-i\theta/2} & 0\\\\  0& e^{i\theta/2} \end{bmatrix}, \\\\ &R_x(\theta) = e^{-i\theta X/2} = HR_z(\theta)H = \begin{bmatrix} \cos(\theta/2) & -i\sin(\theta/2)\\\\  -i\sin(\theta/2) & \cos(\theta/2) \end{bmatrix}, \\\\ &R_y(\theta) = e^{-i\theta Y/2} = SHR_z(\theta)HS^\dagger = \begin{bmatrix} \cos(\theta/2) & -\sin(\theta/2)\\\\  \sin(\theta/2) & \cos(\theta/2) \end{bmatrix}. \end{align*}</span></span>

<span data-ttu-id="e13c0-191">Ugyanúgy, mint három rotációt kombinálva, hogy tetszőleges rotációt végezzen három dimenzióban, láthatja a Bloch szférából, hogy bármely egységes mátrix három rotációs folyamatként is írható.</span><span class="sxs-lookup"><span data-stu-id="e13c0-191">Just as any three rotations can be combined together to perform an arbitrary rotation in three dimensions, it can be seen from the Bloch sphere representation that any unitary matrix can be written as a sequence of three rotations as well.</span></span> <span data-ttu-id="e13c0-192">Pontosabban, minden egységes mátrix $U $ esetén létezik a $ \alpha, a \beta, a \gamma, a \delta $, amely $U = e ^ {i\alpha} R_x (\beta) R_z (\gamma) R_x (\delta) $.</span><span class="sxs-lookup"><span data-stu-id="e13c0-192">Specifically, for every unitary matrix $U$ there exists $\alpha,\beta,\gamma,\delta$ such that $U= e^{i\alpha} R_x(\beta)R_z(\gamma)R_x(\delta)$.</span></span> <span data-ttu-id="e13c0-193">Így a $R _z (\theta) $ és a $H $ is egy Universal Gate-készletet alkot, bár nem különálló készlet, mert a $ \theta $ bármilyen értéket igénybe vehet.</span><span class="sxs-lookup"><span data-stu-id="e13c0-193">Thus $R_z(\theta)$ and $H$ also form a universal gate set although it is not a discrete set because $\theta$ can take any value.</span></span> <span data-ttu-id="e13c0-194">Emiatt, és a kvantum-szimuláció alkalmazásai miatt az ilyen folyamatos kapuk elengedhetetlenek a kvantum-számításokhoz, különösen a Quantum algoritmus tervezési szintjén.</span><span class="sxs-lookup"><span data-stu-id="e13c0-194">For this reason, and due to applications in quantum simulation, such continuous gates are crucial for quantum computation, especially at the quantum algorithm design level.</span></span> <span data-ttu-id="e13c0-195">A hibatűrő hardverek megvalósításához végső soron különálló kapuk lesznek lefordítva, amelyek szorosan megközelítik ezeket a rotációs folyamatokat.</span><span class="sxs-lookup"><span data-stu-id="e13c0-195">To achieve fault-tolerant hardware implementation, they will ultimately be compiled into discrete gate sequences that closely approximate these rotations.</span></span>
